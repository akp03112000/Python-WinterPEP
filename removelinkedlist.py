from pkg_resources import non_empty_lines


class Node
    class ll:
        def__init__(self)
        self.head=none

        def append(self,value)
            nn=node(value)
            if self.head==none
                self.head==nn
                else
                curr=self.head
                while curr.next
                    curr=curr